# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# Excluding for now -- may want to add it in the future
vendor/**

# swig generated files
third_party/occt/libocct_wrap.*
third_party/occt/occt.go
third_party/occt/occt_gc.c
third_party/planegcs/libplanegcs_wrap.*
third_party/planegcs/planegcs.go
third_party/planegcs/planegcs_gc.c
